# ============================================
# ECHO INTUNE - Frontend Environment Variables
# ============================================
# 
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control
#

# ============================================
# API ENDPOINTS
# ============================================
# Backend API URL
VITE_API_URL=http://localhost:5002/api
# For production: VITE_API_URL=https://api.yourdomain.com/api

# ML Service API URL (optional - for advanced analytics)
VITE_ML_API_URL=http://localhost:5001/api
# For production: VITE_ML_API_URL=https://ml.yourdomain.com/api

# ============================================
# NOTES FOR VITE
# ============================================
# - All Vite environment variables must be prefixed with VITE_
# - Only VITE_ prefixed variables are exposed to the client-side code
# - Never store sensitive secrets in frontend .env (they will be publicly visible)
# - After changing .env, restart the dev server for changes to take effect
